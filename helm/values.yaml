replicas:
  server: 1
  client: 3

experiment: "mnist"

server:
  image: "fed-server:latest"
  imagePullPolicy: "Always"
  port: 12345
  numberOfRounds: 8

client:
  image: "fed-client:latest"
  imagePullPolicy: "IfNotPresent"
  port: 12345
  serverHost: "federated-learning-server-svc"
  model: "src.models.model1"
  datasetLoader: "src.dataLoaders.mnist.datasetLoader"
  loss: "CategoricalCrossentropy"
  lossParams: "{}"
  metric: "CategoricalAccuracy"
  metricParams: "{}"
  optimizer: "Adam"
  optimizerParams: '{"learning_rate": 0.001}'
  numClasses: 10
  batchSize: 32
  trainEpochs: 5

volumes:
  logs:
    name: logs-volume
    mountPath: /app/output/logs
    size: 1Gi
    storageClass: manual
    hostPath: /output/logs

  evaluations:
    name: evaluations-volume
    mountPath: /app/output/experiments
    size: 1Gi
    storageClass: manual
    hostPath: /output/evaluations